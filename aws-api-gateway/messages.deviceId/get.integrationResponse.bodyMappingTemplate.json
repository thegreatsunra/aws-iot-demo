#set($inputRoot = $input.path('$'))
{
  "messages": [
    #foreach($elem in $inputRoot.Items) {
      "id": "$elem.id.S",
      "messageId": "$elem.id.S",
      "at": "$elem.at.S",
      "count": "$elem.count.N",
      "countIsPrime": "$elem.countIsPrime.BOOL",
      "lat": "$elem.lat.N",
      "lng": "$elem.lng.N",
      "topic": "$elem.topic.S",
      "deviceId": "$elem.deviceId.S"
    }#if($foreach.hasNext),#end
#end
  ]
}
